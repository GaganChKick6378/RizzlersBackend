# Application Configuration
spring.application.name=rizzlers-backend
server.port=8080
server.servlet.context-path=/api

# Development Environment Indicator
# REMOVED: spring.profiles.active=dev
application.environment=Development

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://database-kdu.czpwqpnfk9dp.ap-south-1.rds.amazonaws.com:5432/Database_10_dev
spring.datasource.username=Team_10
spring.datasource.password=Password10
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Disable SQL initialization for production/development
spring.sql.init.mode=never

# Logging Configuration
logging.level.root=INFO
logging.level.com.kdu.rizzlers=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# API Gateway Configuration
server.forward-headers-strategy=FRAMEWORK
spring.cloud.gateway.enabled=false

# CORS Configuration
application.cors.allowed-origins=http://localhost:3000,https://drld61kimwwfp.cloudfront.net,https://38cl2iacig.execute-api.ap-south-1.amazonaws.com
application.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
application.cors.allowed-headers=Origin,Content-Type,Accept,Authorization,X-Requested-With
application.cors.max-age=3600 