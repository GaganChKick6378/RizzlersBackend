# Application Configuration
spring.application.name=rizzlers-backend
server.port=8080
server.servlet.context-path=/api

# QA Environment Indicator
# REMOVED: spring.profiles.active=qa
application.environment=QA

# PostgreSQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL_QA}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Initialize schema and data
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# Logging Configuration
logging.level.root=INFO
logging.level.com.kdu.rizzlers=INFO
logging.level.org.hibernate.SQL=INFO

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# CORS Configuration
application.cors.allowed-origins=http://localhost:3000,https://d1yi08pshszdit.cloudfront.net/,https://d1yi08pshszdit.cloudfront.net
application.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
application.cors.allowed-headers=Origin,Content-Type,Accept,Authorization
application.cors.max-age=3600 

#For graph ql
# Enable GraphiQL UI
graphql.graphiql.enabled=true
graphql.servlet.mapping=/graphql
graphql.graphiql.path=/graphiql
graphql.playground.enabled=true
graphql.playground.endpoint=/graphql

# GraphQL Configuration
graphql.endpoint=${GRAPHQL_ENDPOINT}
graphql.api-key=${GRAPHQL_API_KEY}

#For graph ql
# Enable GraphiQL UI
graphql.graphiql.enabled=true
graphql.servlet.mapping=/graphql
graphql.graphiql.path=/graphiql
graphql.playground.enabled=true
graphql.playground.endpoint=/graphql

# GraphQL Configuration
graphql.endpoint=${GRAPHQL_ENDPOINT}
graphql.api-key=${GRAPHQL_API_KEY}